import * as authService from '../services/authService.js'

export async function login(req, res) {
  // TODO: const { perfil, senha } = req.body
  // TODO: const token = await authService.autenticar(perfil, senha)
  // TODO: res.json({ token })
}

export async function logout(req, res) {
  // TODO: const token = req.headers.authorization
  // TODO: await authService.encerrarSessao(token)
  // TODO: res.status(204).send()
}
